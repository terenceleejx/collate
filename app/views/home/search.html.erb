<div class="table-responsive">
  <table class="table">
  	<thead>
  		<tr>
  			<th>Topic</th>
  			<th>Value</th>
  			<th>Type</th>
  			<th>Date</th>
  			<th>Source</th>
  		</tr>
  	</thead>
  	<tbody>
			<% @topics.each do |topic| %>
 			  <% topic.quantities.each do |quantity| %>
 			  	<tr>
 			  		<td>
 			  			<% quantity.topics.each do |topic| %>
 			  		    <%= topic.name %>
 			  		  <% end %>
 			  		</td>
 			  		<td>
 			  			<% if quantity.unit_id == 1 %>
 			  				<%= quantity.number*100 %> %
 			  			<% else %>
 			  				<%= quantity.number %>
 			  			<% end %>
 			  		</td>
 			  		<td>
 			  			<% @quantity_types = QuantityType.find(quantity.id) %>
 			  			<% @quantity_types.name %>
 			  		</td>
 			  		<td><%= quantity.date.strftime("%B %d, %Y") %></td>
 			  		<td>
 			  			<% @sources = Source.find(quantity.id) %>
 			  			<a href="<%= @sources.URL %>"><%= @sources.title %></a>
 			  		</td>
 			  	</tr>
 			  <% end %>
			<% end %>
  	</tbody>
  </table>
</div>

<!--

Quantity:

topics, value, quantity type

Charts:

embed actual charts

-->